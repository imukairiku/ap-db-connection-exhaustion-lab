## 5. 暫定復旧する

調査で特定した旧APの残留接続だけを安全に終了してください。
PIDの再照合を行い、既に消えたPIDは処理済みとし、新AP・管理用接続を巻き込まないでください。
DB全体を再起動せず、`sudo bash scripts/scenario-business-check.sh` で新APの業務COMMITを確認してください。
判定はコマンド履歴ではなく、実際に残る接続、業務結果、DBの継続性を見ます。
